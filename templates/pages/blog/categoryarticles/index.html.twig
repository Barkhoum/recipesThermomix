{% extends 'base.html.twig' %}


{% block title %}{{ categoryarticles.name }}{% endblock %}

{% block body %}
    <main class="container">
        <div class="row g-5">
            <div class="col">
                <h3 class="pb-4 mb-4 fst-italic border-bottom">{{ categoryarticles.name }}</h3>
                <div id="articles-list">
                    <img src="{{ categoryarticles.imagePath }}" alt="">


                </div>
            </div>
        </div>
    </main>
{% endblock %}

